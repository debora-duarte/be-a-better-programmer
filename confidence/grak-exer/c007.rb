# This is Ruby code

# It also makes use of arrays - Ruby arrays in this case

# Pay attention that Ruby also tells you in which line the error is occuring like this:

# (repl):15:1:in '/'

# This 15 right after (repl) means the error is occuring at the line 15

my_numbers = [1, 10, 28, 40]

a = my_numbers[0] + my_numbers[1] + my_numbers[2] - my_numbers[3]

puts 78/a
puts "It workeeeeeeeeeed!"

# divisão por 0
